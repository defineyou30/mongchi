# Initial Implementation Plan

## Pass 1: Project Scaffold

- Create Expo + TypeScript mobile app shell in `apps/mobile`.
- Create shared domain package in `packages/shared`.
- Create API placeholder in `services/api`.
- Create AI worker placeholder in `workers/ai`.
- Document security and provider boundaries.

## Pass 2: Mock First Session

- Persist local onboarding draft. Done for the native MVP slice with AsyncStorage.
- Add lightweight pet name, species, personality, and talking-style forms. Done.
- Add mock photo review and consent state. Done with native local photo selection plus sample fallback.
- Drive generation/hatching through mock job states. Done with completion, failure, and retry states.
- Show pet reveal with mock generated asset metadata. Done.
- Enter terrarium with starter care state and starter inventory. Done.

## Pass 3: Local Daily Loop

- Wire care actions to local state transitions. Done.
- Select authored reaction bubbles from local rules. Done.
- Add anti-repeat history persistence. Done through persisted recent reaction history.
- Add idle walk start/return/claim state. Done.
- Add starter item placement. Done for starter items and walk reward.

## Pass 4: Backend Integration Preparation

- Convert placeholder endpoint contracts into a real API surface. Initial pure API service boundary now covers auth context, current-user onboarding state, pet list/create/update/delete, ownership guards, source-photo upload URL issuance, upload completion validation, generation-job creation preconditions, generated asset listing, and original-photo deletion metadata.
- Add typed DTO mappers in the mobile app. Initial shared mobile contracts now cover create-pet, local source-photo validation, signed upload URL request preparation, upload method/header metadata, generation job request, accept-job request, and care action request payloads.
- Add mobile production API client boundary. The app now has a provider-neutral fetch client with base URL validation, session-token resolution, auth-token injection, provider-token expiry/refresh support, typed upload/generation/care/chat methods, and safe API/network error mapping; daily loop and first-session generation runtimes can use the API boundary in integration builds while production auth provider accounts and deployed storage secrets remain unmounted.
- Add auth, signed upload URL, generation job, and care action integration behind mocks. API auth/upload/generation boundaries are implemented as tested pure service code, API mounts can reject default mock auth/storage headers and signing paths for production-style deployments, API Node runtime has tested async `ApiSessionVerifier` and `PrivateStorageSigner` injection points plus a tested RS256 JWT/JWKS session verifier adapter with runtime config and S3-compatible storage signer with runtime config, mobile API requests now resolve tokens through a tested session-token boundary, and mobile first-session generation uses a tested signed upload transport for HTTP(S) upload URLs; real auth provider account values, mobile session refresh, and storage deployment values still need production wiring.
- Add framework-neutral HTTP route adapter for the mock API boundary. Done for current `/v1` mobile routes, including current user, pet profile CRUD, photo upload metadata, generation job reads/retry/accept, generated asset listing, generated asset signed-read URL issuance, privacy deletion requests, premium chat, and commerce restore/verify contracts; production deployment and middleware still need integration.
- Add Node HTTP runtime adapter for the API boundary. Done with JSON parsing, body-size limits, method allowlisting, optional CORS allowlisting, optional per-client in-memory rate limiting, safe structured request logging without headers/query/body, safe JSON-line operational logger helpers, unauthenticated liveness/readiness probes with Postgres database readiness wiring, loopback integration tests, tested async session/storage/purchase verifier injection points, a tested mock-service JSON snapshot store for local/runtime restoration, an initial Postgres SQL migration for API-owned state, a tested migration runner boundary, a tested `pg` client adapter, tested current-user/pet profile, photo/generation/asset metadata with worker-safe job claiming/status transitions, daily-loop, premium-chat, commerce, privacy-deletion, and outbox Postgres repositories, a tested Postgres repository bundle, a tested async-only current-user/pet-profile/source-photo/generation-job/generated-asset-read/accept/daily-loop/chat/commerce/privacy-deletion Postgres API service slice with durable mock generation completion/failure/retry and worker-supplied private generated asset metadata, a tested generation worker runtime, a tested Postgres Node server factory with operational logger request/premium-chat monitor wiring, a tested API server process env/listen/shutdown boundary, tested API `start:*` runtime commands for server and worker processes, a tested S3-compatible storage signer/deleter runtime path, a tested S3-compatible worker storage read/write adapter, a tested privacy deletion worker runner/process runner/processor/runtime-env deployment composer with outbox audit sink wiring, a tested outbox worker runner/process/runtime-env deployment composer, a tested chat retention purge worker runtime-env deployment composer, and a tested snapshot-to-Postgres repository plan; production deployment, deployed log shipping/retention and alert sinks, scheduler infrastructure, infrastructure-level distributed rate limits, concrete auth/store/provider adapters, and deletion alerting still need integration.
- Add backend daily loop endpoints behind the API boundary. Done for authored item catalog, active versioned reaction catalog, user inventory and placement, owned care state, care actions, walk start, walk return gating, reward claim, mobile client methods, and the Postgres-backed request service slice. Mobile terrarium runtime now has an optional API-backed integration mode; production auth/session persistence still needs integration.
- Add worker upload intake safeguards. Initial worker boundary now validates JPEG/PNG/WebP signatures, parses and limits header dimensions, rejects content-type mismatch and unreadable files with safe messages, verifies JPEG terminal EOI and scan payload structure, PNG chunk CRC/IDAT/IEND structure, inflates PNG IDAT data to validate raster scanline lengths and filter bytes, verifies WebP RIFF chunk bounds and image chunk presence, runs Sharp-backed full source-photo pixel decode in the async worker path, enforces size limits, strips JPEG APP1 EXIF metadata before provider-safe bytes, runs a tested source-photo safety precheck contract before provider generation with a provider classifier injection boundary and tested OpenAI source-photo safety adapter, applies a structured generation quality gate with runtime-configurable thresholds and a tested OpenAI generation quality evaluator for provider/CV output signals, includes a tested OpenAI image edit provider adapter, one-job worker runtime, batch runner, and runtime composition helpers for repository-backed claim, OpenAI adapter execution, asset storage, and completion/failure persistence, and has a tested S3-compatible storage adapter for original-photo reads and generated-asset writes.
- Add generation job polling behind the hatching UI. The mobile hatching screen now uses the API poll boundary in configured integration builds and the tested prototype polling boundary by default, with scheduled polls, manual reduce-motion progression, terminal handling, and progress snapshots.
- Add generation lifecycle endpoints behind the mock API boundary. The pure service now has tested job reads, mock poll/progression, mock completion, generated asset listing, app-private generated asset signed-read URL issuance, async storage signer integration, accept/activate, quality failure, and retry behavior. The Postgres-backed service now persists mock poll progression for development, can block mock poll auto-completion in production runtime config, and persists durable completion/failure/retry state plus worker-supplied private generated asset metadata through the repository bundle. The worker package now has a tested repository-backed execution runtime for claim, OpenAI-backed source-photo safety precheck, OpenAI image edit provider adapter output, OpenAI generation quality evaluation, runtime-configurable quality gate, asset storage, and completion/failure persistence, plus a scheduler-friendly process runner, tested S3-compatible storage adapter, and OpenAI runtime composition helper for original-photo reads, generated-asset writes, and adapter wiring. The API package now has a tested OpenAI/Postgres generation worker runtime-env deployment composer for the repository bundle and scheduler process env. The S3-compatible signer can issue generated-asset read URLs for private `s3://` metadata. Deployed worker provider/S3 storage credentials, production scheduler infrastructure, and threshold value calibration still need deployment integration.
- Keep AI generation and premium chat backend-only.

## Pass 5: Safety, Commerce, And Release Readiness

- Add delete original photo, delete pet, delete chat history, and delete account flows. Local destructive photo, chat, and pet reset actions now require native confirmation before clearing prototype state; configured API integration builds route original-photo, chat-history, and pet-data deletion through tested server ownership boundaries before local state is cleared.
- Add premium chat gate and server-verified entitlement checks. API service now has tested active-entitlement enforcement, disclosure requirement, input moderation, provider-output moderation with localized crisis/professional-advice fallback copy, conversation thread read/delete ownership, backend-only local provider fallback, Postgres-backed conversation/message persistence, configurable server-side turn rate limiting, retention-window filtering for thread reads/provider context/rate-limit history, capped provider context, safe no-text provider monitor events routed through the operational logger adapter, a bounded retention purge worker/process/runtime-env deployment, and an OpenAI Responses provider adapter with structured output and safe refusal/provider-unavailable handling; deployed log/alert sinks and scheduler infrastructure still need production integration.
- Add purchase restore path before store account rollout. The mobile Settings restore control now calls the API restore contract in API-backed builds, uses `expo-iap` available purchases to send Android restore purchase tokens as request-scoped verifier metadata, and remains a safe local no-op when checkout is not enabled. API service and mobile client now have tested server-owned product catalog and current-user entitlement read contracts, plus an optional native `expo-iap` checkout gateway, mock purchase verification, verifier-only raw store token transport, idempotent entitlement grants, restore, transaction reuse protection, refund/revocation handling through a secret-guarded server-only webhook route with direct App Store x5c/ES256 JWS verification, a production-style mock purchase disable option, a server-only async `StorePurchaseVerifier` injection point, tested HTTP and direct App Store / Google Play verification adapters with runtime config, and Postgres-backed entitlement/ledger persistence; concrete store product setup, deployed webhook registration, and monitoring still need integration.
- Add QA checks for small/large iOS and Android screens.
- Apply reference-driven mobile art direction. Done as React Native scene composition using separate generated background/item PNG assets, not full-screen reference crops, across Welcome, Photo upload, Pet setup, Hatching, Reveal, Main terrarium, Premium chat, and Walk reward/shop.

## Decisions Held

- No concrete AI provider credentials are mounted in this scaffold. Tested OpenAI image edit, source-photo safety, generation quality, and premium chat adapters plus runtime-configurable quality thresholds exist, but production provider secrets, calibrated threshold values, and deployment wiring are not mounted.
- No real payment or subscription implementation in this scaffold.
- Initial production database migration, migration runner boundary, `pg` Postgres client adapter, current-user/pet profile repository, photo/generation/asset metadata repository with worker-safe job claiming/status transitions, daily-loop care/inventory/walk/reaction repository, premium-chat conversation/message repository, commerce entitlement/purchase-ledger repository, privacy deletion job repository, sanitized outbox event repository, Postgres repository bundle, current-user/pet-profile/source-photo/generation-job/generated-asset-read/accept/daily-loop/chat/commerce/privacy-deletion Postgres API service slice, tested OpenAI premium chat provider adapter, safe operational logger helpers, Postgres Node server factory, API server process env/listen/shutdown boundary, tested API server/worker `start:*` commands, durable mock generation lifecycle persistence, worker-supplied private generated asset metadata persistence, tested generation worker runtime, batch runner, scheduler-friendly process runner, OpenAI/Postgres worker deployment composer, S3/OpenAI runtime composition helpers, tested source-photo safety precheck contract with provider classifier injection, tested OpenAI image edit provider adapter, tested OpenAI source-photo safety classifier adapter, tested OpenAI generation quality evaluator, tested S3-compatible worker storage read/write adapter, tested worker runtime config validation, privacy deletion worker runner/process runner/processor/runtime-env deployment composer with outbox audit sink wiring, outbox worker runner/process/runtime-env deployment composer, chat retention purge worker/process/runtime-env deployment composer, database bootstrap helper, runtime auth/DB/S3 storage config validation, tested JWT/JWKS verifier runtime path, tested S3-compatible storage signer/deleter, and snapshot-to-Postgres repository plan exist; the request runtime still needs threshold value calibration, deployed log/alert sinks, scheduler infrastructure, and deployed auth/storage/provider policies/secrets.
- No manual generated-pet editor in the first implementation path.
